---
interface Props {
	date: Date;
}

let { date } = Astro.props;
date = new Date(date)
---

<time class="" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			weekday: 'short',
			month: 'short',
			day: 'numeric',
			year: 'numeric',
		})
	}
</time>
